# G14 — Проверки

## Базовая версия (828457e, до изменений)

- macOS 15.6.1 arm64, Swift 6.1.2: `swift build` успешно.
- `swift test` на Mac: блокировка среды — CLT не содержит XCTest; исходники тестов не менялись для обхода.
- Linux claudebot-server, Swift 6.0.3: `swift test` 58/58, 0 failures.
- Установленный /Applications/MacLayoutSwitcher.app: 1.2.0. Пользователь ещё не проверял Enter/Shift+Enter/быстрый набор.
- Удалённый основной checkout совпадал с 828457e, изменений нет; каталог релизного архива доступен для записи.

## Выполнено до публикации

- Реальный нативный GigaAM: русский WAV, WAV дольше 25 секунд, тишина, повреждённый WAV и модель.
- Загрузчик: закреплённые SHA256/размеры, отмена, ошибка, cache после перезапуска; отсутствие сетевого fallback в ASR.
- Core XCTest: удержание/отпускание, autorepeat, отмена до разрешения, поздний результат, повтор после ошибки, конфликты.
- Два независимых ревью: соответствие требованиям и качество реализации; исправить существенные находки.
- Финальная Mac release-сборка, codesign .app/helper, .dmg; Linux весь XCTest на той же версии.
- VERSION, push, серверный git archive; проверить содержимое публичного архива.

## Ручная приёмка на Mac (не подменять автоматическими тестами)

- Первое включение: модель → разрешение микрофона → удерживать и говорить → отпустить → текст появился в поле.
- Esc во время записи/распознавания, отпускание до ответа на запрос разрешения, отзыв разрешения.
- Смена приложения/поля во время распознавания — результат показан отдельно.
- Быстрый набор во время вставки, Enter/Shift+Enter, исходный Option/автозамена/сниппеты.
- Реальный голос пользователя и микрофон не записывались в рамках автоматической проверки.

## T16 — независимая проверка backend (Mac, 2026-09-16)

Временный harness `work/g14-backend-checks/main.swift` скомпилирован напрямую со SpeechModelStore/SpeechRecognitionProcess (не подменяет реальный ASR). 12 проверок прошли:

- отсутствующая модель сообщает missingModel; файлы правильных размеров с неверным SHA256 отвергаются;
- отменённая загрузка возвращает cancellation и удаляет staging;
- корректный JSON helper читается, исходный WAV остаётся у вызывающей стороны;
- некорректный JSON и no_speech становятся ошибками;
- зависший helper завершается по таймауту и отмене, чрезмерный stdout ограничивается;
- настоящие четыре файла (232,576,763 байта) загружены сервисом из закреплённого релиза с прогрессом и SHA256, повторная полная проверка cache успешна.

Тестовая модель — в игнорируемом work/g14-model-test, пользовательская Application Support не менялась. Проверка настоящего распознавания проводится отдельно.

## Промежуточные проверки T17 и настоящего ASR

- Isolated Linux core snapshot: 67 XCTest (58 прежних + 9 диктовка), все прошли. Рабочий checkout сервера не менялся; QA в отдельном временном каталоге.
- Нативный helper: 4-секундный русский WAV распознан за 1.77 секунды; 5 секунд тишины → no_speech, без текста. Сеть запрещена sandbox-exec.
- 69.265-секундная сборка из 15 разных русских фраз: распознавание завершилось за 6.54 секунды; дословное совпадение хвоста с отдельной фразой не прошло (различия контекста/ASR), поэтому отдельно проверена потеря хвоста.
- 36-секундный WAV, речь только после 32-й секунды: результат в точности совпал с отдельной 4-секундной записью. Проверка обрыва длинного аудио пройдена при запрещённой сети. Это не оценка точности на речи пользователя.

## Итоговая проверка v1.3.0

- Два независимых ревью Spec/Standards: единственная находка P2 — потерянный keyUp при паузе/записи хоткея. Исправлена полным reset физического состояния; оба ревьюера подтвердили исправление. Других actionable findings нет.
- Полный исходный snapshot финального кода: Linux 71 XCTest / 0 failures (58 прежних + 13 новых). Mac debug/release успешно.
- Повреждённый WAV и отсутствующая модель проверены настоящим helper: ненулевой exit, структурированная ошибка, текста нет.
- DMG пересобран после исправления P2; смонтирован read-only. Внутри VERSION 1.3.0, NSMicrophoneUsageDescription, Applications link. codesign --verify --deep --strict прошёл для приложения/вложенного helper. Helper из смонтированного DMG распознал настоящий русский WAV под запретом сети.
- Артефакт: MacLayoutSwitcher-1.3.0.dmg, 10,879,706 bytes; SHA256 1088885cd049144b6324ec7934d8c3599a7cc440ed883cb302c738e651380554.
- Важная средовая находка: iCloud FileProvider добавляет FinderInfo к .app в Documents после подписи. Для финальной сборки использован MLS_DIST_DIR во временном каталоге; упаковщик очищает метаданные staging и проверяет подпись перед созданием DMG.
- Настоящий микрофон/внешнее поле/TCC пользователя не проверялись. Установленный /Applications/MacLayoutSwitcher.app не заменялся автоматически.
